#ifndef _sum_h
#define _sum_h

#include <stdio.hg>

#define SUM(x, y)
/**
 * main - entry point
 *
 * Return: 0
 */

int main(void)
{
	int s;

	s = SUM(x, y);
	printf("%d\n", s);
	return (0);
}

#endif
