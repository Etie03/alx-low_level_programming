#include "main.h"
/**
 * add -  a function that adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 *
 * Return: result of the addition
 */

int add(int a, int b)
{
return (a + b);
}
